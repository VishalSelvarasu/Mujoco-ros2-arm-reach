<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>mujoco_arm_bridge</name>
  <version>0.1.0</version>
  <description>
    ROS 2 node that runs a trained MuJoCo RL policy (PPO/SAC) for UR5e
    reach tasks and exposes it through standard ROS 2 topics, so the
    policy can be driven the same way a real robot controller would be.
  </description>
  <maintainer email="you@example.com">Your Name</maintainer>
  <license>MIT</license>

  <depend>rclpy</depend>
  <depend>sensor_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>std_msgs</depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
